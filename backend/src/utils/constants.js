
const userRolesEnum = {
    USER: "user",
    ADMIN: "admin",
}

const userRoles = Object.values(userRolesEnum);

const difficultyLevelsEnum = {
    EASY: "easy",
    MEDIUM: "medium",
    HARD: "hard",
}

const difficultyLevels = Object.values(difficultyLevelsEnum);

export { userRolesEnum, userRoles, difficultyLevelsEnum, difficultyLevels };